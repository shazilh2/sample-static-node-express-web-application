apiVersion: v1
kind: Service
metadata:
  name: nodeapp
  labels:
    app: nodeapp
spec:
  type: NodePort
  selector:
    app: nodeapp
  ports:
    - port: 3000       # Service port
      targetPort: 3000  # Container port (Tomcat)
      protocol: TCP
      nodePort: 31000
